<div class="devise-form">
<h2>Log in</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

    <%= f.button :submit, "Log in", class: "btn-space-station" %>
<% end %>

<%= render "devise/shared/links" %>
</div>
